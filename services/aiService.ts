// Fase 5 — Implementado pelo Engenheiro
// Responsabilidade: interpretar mensagens WhatsApp e retornar JSON estruturado de intent

import type { AIResponse } from '@/types'

export async function parseIntent(_message: string): Promise<AIResponse> {
  // TODO: implementar chamada OpenAI com prompt estruturado
  // Retorna sempre JSON validado — nunca confiar na IA diretamente sem validação
  throw new Error('aiService.parseIntent: não implementado — Fase 5')
}
